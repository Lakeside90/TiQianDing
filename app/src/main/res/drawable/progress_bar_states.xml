<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:startColor="@color/window_bg"
                android:centerColor="@color/window_bg"
                android:endColor="@color/window_bg"
                />
        </shape>
    </item>

    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:startColor="@color/common_green"
                    android:centerColor="@color/common_green"
                    android:endColor="@color/common_green"
                    />
            </shape>
        </clip>
    </item>

</layer-list>